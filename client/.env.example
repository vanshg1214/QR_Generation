# Leave unset for local dev (Vite's proxy forwards /api and /r to the local server).
# Set this in Vercel's project settings only if the dashboard is deployed separately
# from the API server, e.g. https://qr-generation-ag74.onrender.com
VITE_API_BASE_URL=
